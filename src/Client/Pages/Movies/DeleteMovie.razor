<Dialog Id="@Id" Title="Delete movie">
    <Body>
        @if (Movie is null)
        {
            <p>No movie to edit...</p>        
        }
        else
        {
            <p class="text-danger">Do you really want to delete this movie? This action cannot be undone.</p>
            <p>
                Title: <span class="font-bold">@Movie.Title</span>
            </P>                 
        }
    </Body>
    <Footer>
        <button type="button" class="btn btn-secondary" @onclick="Cancel" data-bs-dismiss="modal">Cancel</button>
        <button type="button" class="btn btn-danger" @onclick="Delete" disabled=@IsTaskRunning>Delete</button>
    </Footer>
</Dialog>
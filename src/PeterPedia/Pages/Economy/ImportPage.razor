@page "/economy/import"

<h4 class="mt-2">Import transaction</h4>

<EconomyMenu Page="Models.EconomyPage.Import"></EconomyMenu>

<div class="mb-3">
    <label for="account">Account type</label>
    <select class="form-select" id="account" @bind="@AccountType">
        <option value="0">Select account</option>
        @foreach (var type in AccountTypes)
        {
            <option value="@type">@type</option>
        }
    </select>
</div>
<div class="mb-3">
    <label for="file">Files</label>
    <InputFile class="form-control" type="file" id="file" OnChange="@LoadFiles" multiple />
</div>
<div class="mb-3">
    <button type="button" class="btn btn-success" @onclick="UploadAsync">Upload file</button>
</div>

<h1>TODO: Show import errors?</h1>


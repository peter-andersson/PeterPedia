@page "/error"

<h4 class="mt-2">Errors</h4>

<ul class="list-group">
@foreach (ErrorEF error in Errors)
{
    <li class="list-group-item">@($"[{error.Module}] {error.Timestamp}: {error.Error}")</li>
}
</ul>

@page "/readlist"

<h2>Want to read</h2>

<div class="card-group">
    <div class="row col-12">
        @foreach (var book in BookList)
        {
            <BookView Book=@book ReturnUrl="readlist"></BookView>
        }
    </div>
</div>
@page
@model PeterPedia.Server.Pages.IndexModel

<div class="row mt-2">
    @foreach (var link in Model.Links)
    {
        <div class="col-12 col-md-6 col-xxl-4">
            <a class="fs-2 link-light" href="@link.Url">@link.Title</a>
        </div>
    }
</div>
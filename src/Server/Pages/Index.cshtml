@page
@model PeterPedia.Server.Pages.IndexModel

<div class="grid grid-flow-row grid-cols-2 gap-4 md:grid-cols-4">
    @foreach (var link in Model.Links)
    {
        <a class="text-xl" href="@link.Url">@link.Title</a>
    }
</div>